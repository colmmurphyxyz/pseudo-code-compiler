LIST-SEARCH(L, k)
    x = L.head
    while x != NIL and x.key != k
        x = x.next
    return x

LIST-SEARCH'(L, k)
    L.nil.key = k
    x = L.nil.next
    while x.key != k
        x = x.next
    if x == L.nil
        return NIL
    else return x
